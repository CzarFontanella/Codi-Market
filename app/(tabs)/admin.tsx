import { View, Text } from 'react-native';

export default function AdminScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ fontSize: 20, fontWeight: '600' }}>Área Secreta do Admin 👀</Text>
    </View>
  );
}
